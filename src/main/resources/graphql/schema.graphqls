type User {
    id: ID!
    username: String!
    email: String!
}

type Query {
    allUsers: [User]
    userById(id: ID!): User
}

type Mutation {
    createUser(username: String!, email: String!, password: String!): User
}
